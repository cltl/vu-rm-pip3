#!/bin/bash
#
# Module calls: opin
#----------------------------------------------------


workdir=$(cd $(dirname "${BASH_SOURCE[0]}") && cd ../.. && pwd)
modulesdir=$workdir/components/forked
cd $modulesdir/opinion_miner_deluxePP
python tag_file.py -polarity -d hotel
cd $workdir
